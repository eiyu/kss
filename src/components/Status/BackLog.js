import React from 'react'

// I don't get the idea of back log (why don't we just straight to todo?)

const BackLog = (props) => (
  <div>
    <div className="card-status"> Back-Log </div>
    <div className="card-body">get cards from state</div>
  </div>
)

export default BackLog
